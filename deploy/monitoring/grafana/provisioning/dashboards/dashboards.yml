# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'SuperInsight Dashboards'
    orgId: 1
    folder: 'SuperInsight'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards